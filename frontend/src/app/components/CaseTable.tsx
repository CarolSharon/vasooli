import Link from "next/link";
import { ProvenanceBadge } from "./ProvenanceBadge";
export type CaseRow = { id: number; case_reference: string; customer_name: string; amount: number; status: string; workflow?: string; outcome_provenance?: string };
export function CaseTable({ rows, title }: { rows: CaseRow[]; title: string }) { return <main className="min-h-screen bg-slate-50 p-8 text-slate-950"><Link href="/" className="text-emerald-700">← Dashboard</Link><h1 className="my-6 text-3xl font-bold">{title}</h1><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-left"><thead className="bg-slate-100"><tr>{["Case", "Customer", "Amount", "Status", "Workflow", "Provenance"].map(x => <th className="p-3" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map(row => <tr className="border-t" key={row.id}><td className="p-3"><Link className="text-emerald-700" href={`/cases/${row.id}`}>{row.case_reference}</Link></td><td className="p-3">{row.customer_name}</td><td className="p-3">₹{row.amount.toFixed(2)}</td><td className="p-3">{row.status}</td><td className="p-3">{row.workflow ?? "—"}</td><td className="p-3"><ProvenanceBadge value={row.outcome_provenance} /></td></tr>)}</tbody></table></div></main>; }
